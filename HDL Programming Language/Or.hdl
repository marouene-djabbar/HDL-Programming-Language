CHIP Or {
    IN c, f;
    OUT out;

    PARTS:
    Nand(a=c, b=c, out=notc);
    Nand(a=f, b=f, out=notf);
	Nand(a=notc, b=notf, out=notcNandnotf)
	Nand(a=notcNandnotf, b=notcNandnotf, out=out);
	
}